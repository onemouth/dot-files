[user]
	name = Li-Ting Tsai
	email = liting@17.media
[color]
    diff = auto
    status = auto
    branch = auto
    log = auto
[pager]
    diff = diff-so-fancy | less --tabs=1,5 -RFX
    show = diff-so-fancy | less --tabs=1,5 -RFX
